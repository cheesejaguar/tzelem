# Minimal requirements for Vercel deployment
# Heavy ML/AI dependencies removed to stay under 250MB limit

fastapi==0.115.6
uvicorn[standard]==0.34.0
aiohttp==3.12.15
python-dotenv==1.0.1
pydantic==2.10.6
pydantic-settings==2.7.1
requests>=2.31.0

# Only include essential dependencies that don't exceed size limit
# Remove these heavy packages:
# - pipecat-ai==0.0.80 (requires numpy, scipy, onnxruntime, etc.)
# - onnxruntime>=1.22.1 (16.5 MB)
# - pipecat-ai-flows>=0.0.18
# - convex==0.6.0
# - agentmail>=0.0.53
# - numba==0.61.2
# - daily-python (for voice features)